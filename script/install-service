#!/usr/bin/env bash
set -e

SERVICE_NAME="wyoming-onnx-asr"
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

# Detect the actual user even if run with sudo
REAL_USER=${SUDO_USER:-$(logname 2>/dev/null || echo $USER)}
REAL_GROUP=$(id -gn "$REAL_USER")

echo "🛠 Installing $SERVICE_NAME for user: $REAL_USER"

sudo bash -c "cat > /etc/systemd/system/$SERVICE_NAME.service" <<EOF
[Unit]
Description=Wyoming ONNX ASR Server
Wants=network-online.target
After=network-online.target

[Service]
Type=simple
User=$REAL_USER
Group=$REAL_GROUP
WorkingDirectory=$PROJECT_ROOT

# --- GPU / CUDA visibility ---
Environment="LD_LIBRARY_PATH=/usr/local/cuda/lib64:/usr/lib/x86_64-linux-gnu"
Environment="CUDA_VISIBLE_DEVICES=0"

# --- Python stability ---
Environment="PYTHONUNBUFFERED=1"
Environment="OMP_NUM_THREADS=1"

# --- ONNX Runtime tuning (mirrors ORT_CONFIG defaults) ---
Environment="ORT_LOGGING_LEVEL=3"
Environment="ORT_DISABLE_THREAD_AFFINITY=1"
Environment="ORT_NUM_THREADS=1"

# --- OPTIONAL: override model here ---
# Examples:
# ExecStart=$PROJECT_ROOT/script/run --model parakeet-v3
# ExecStart=$PROJECT_ROOT/script/run --model parakeet-v2
# ExecStart=$PROJECT_ROOT/script/run --model canary

ExecStart=$PROJECT_ROOT/script/run

Restart=always
RestartSec=5
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
EOF

# Permissions + reload
sudo chmod 644 "/etc/systemd/system/$SERVICE_NAME.service"
sudo systemctl daemon-reload
sudo systemctl enable "$SERVICE_NAME"
sudo systemctl restart "$SERVICE_NAME"

echo "-------------------------------------------------------"
echo "✅ $SERVICE_NAME installed and started!"
echo "Check status: sudo systemctl status $SERVICE_NAME"
echo "View logs:    journalctl -u $SERVICE_NAME -f"
echo "-------------------------------------------------------"
