#!/usr/bin/env bash
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
VENV_PYTHON="$PROJECT_ROOT/.venv/bin/python3"

# Safety Check
if [ ! -x "$VENV_PYTHON" ]; then
    echo "❌ Error: Virtual environment not found. Run setup first."
    exit 1
fi

# --- ONNX / PERFORMANCE SAFETY FLAGS ---
export OMP_NUM_THREADS=1
export MKL_NUM_THREADS=1

# --- ORT consistency with Python ORT_CONFIG ---
export ORT_LOGGING_LEVEL=3
export ORT_DISABLE_THREAD_AFFINITY=1
export ORT_NUM_THREADS=1

# --- CUDA stability ---
export CUDA_VISIBLE_DEVICES=0

# --- HuggingFace / filesystem stability ---
export HF_HUB_DISABLE_SYMLINKS=1

echo "🚀 Starting Wyoming ONNX ASR..."
echo "📁 Project: $PROJECT_ROOT"

cd "$PROJECT_ROOT"
exec "$VENV_PYTHON" wyoming_onnx_asr.py "$@"
